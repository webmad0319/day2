const danimaths = {
    randomIntFromInterval(min, max) // min and max included
    {
        return Math.floor(Math.random() * (max - min + 1) + min);
    },
    calculateRandomColor() {

    }
}

utils.randomIntFromInterval(10, 100)
utils.calculateRandomColor()